# Main Terraform configuration
# This file serves as the entry point for the otaku.lt infrastructure

# Note: GitHub authentication is handled via environment variables
# Set: export GITHUB_TOKEN="$(gh auth token)"
# Or provide token directly: export GITHUB_TOKEN="your_token"
